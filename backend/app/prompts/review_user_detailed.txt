Review this {language} code. Scores range from 0 to 100. Return only one JSON object with keys in this order: score, metrics, summary, rationale, findings. After score, write metrics as an object with keys Correctness, Security, and Maintainability, in that order. Close all three metric values before writing summary. Each metric value must have an integer score, a description, and a snippet. For each metric, write 3-4 sentences in description that explain why that numeric score was given, citing specific evidence from the submitted code. Do not write a one-sentence description. Do not define what Correctness, Security, or Maintainability means. The snippet must contain the most relevant exact 1-3 lines from the submitted code; use an empty string only when no relevant snippet exists. Include a concise rationale with the evidence used for the assessment. Each finding must use a real 1-based line number. If there are no concrete issues, return an empty findings array. Never omit a metric to make room for findings.
